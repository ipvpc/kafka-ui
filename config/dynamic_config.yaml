# One broker at kafka.alpha5.finance:9092
# ("clusters" in Kafka config = connection entries; one entry for a single-node server)

kafka:
  clusters:
    - name: kafka
      bootstrap-servers: kafka.alpha5.finance:9092
